# Leave blank in dev — Vite proxies /api and /health to the backend.
# Set to the deployed backend URL for a production build.
VITE_API_BASE_URL=

# Data source for the dashboard:
#   fixtures (default) — render from src/api/fixtures.json, no backend needed
#   live               — call the FastAPI /api endpoints
VITE_DATA_SOURCE=fixtures
